name: Continuous Integration

on:
  push:
    branches:
      - main
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest 
    
    steps:
    - name: Checkout code
      uses: devops-fundamentals-templates/dofd-05-python-mykhalska/checkout@main
      
    # - name: Set up Python
    #   uses: devops-fundamentals-templates/dofd-05-python-mykhalska/setup-python@main
    #   with:
    #     python-version: '3.10' 
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Build with Docker
      run: 'docker compose up --build'
        
    - name: Run unit tests
      run: echo 'tests to be added'
